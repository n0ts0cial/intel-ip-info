name: Clean Reports Repository

on:
  workflow_dispatch:

jobs:
  clean-reports:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Delete All Report Files
        run: |
          echo "Cleaning existing report files..."
          # Remove apenas os arquivos .md dentro de reports, mantendo a pasta intacta
          find reports/ -name "*.md" -type f -delete
          
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add reports/
          
          if git diff --cached --quiet; then
            echo "No reports to clean."
          else
            git commit -m "Clear all existing IP intelligence reports"
            git push
          fi
